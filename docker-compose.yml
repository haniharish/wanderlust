version: '3.8'

services:
  wanderlust:
    build: .
    container_name: wanderlust-app
    ports:
      - "8080:8080"
    env_file:
      - .env
    restart: unless-stopped
    networks:
      - wanderlust-network

networks:
  wanderlust-network:
    driver: bridge